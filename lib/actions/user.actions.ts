"use server";

export const signIn = () => {
  try {
  } catch (error) {
    console.error("Erro", error);
  }
};

export const signUp = (userData: SignUpParams) => {
  try {
    // Create a user account
  } catch (error) {
    console.error("Erro", error);
  }
};
